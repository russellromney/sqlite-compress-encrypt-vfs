# Shared target-dir so turbolite's builds dedupe with cinch-cloud and the
# rest of the sibling repos. Use RUSTC_WRAPPER locally if you want sccache;
# committed config must stay portable for Linux CI and non-Homebrew systems.

[build]
target-dir = "../cinch-target"
incremental = true
